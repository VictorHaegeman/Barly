org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Do not hardcode org.gradle.java.home in the repo (CI runs on Linux/macOS).
# Use JAVA_HOME (JDK 17 recommended) or a user-level Gradle config instead.
